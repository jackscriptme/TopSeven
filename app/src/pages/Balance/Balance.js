import { Box } from '@mui/material';

import Layout from '../../components/Layout';

const Balance = () => {
  return (
    <Layout>
      <Box>Balance</Box>
    </Layout>
  );
};

export default Balance;
