import { Box } from '@mui/material';

import Layout from '../../components/Layout';

const Leagues = () => {
  return (
    <Layout>
      <Box>Leagues</Box>
    </Layout>
  );
};

export default Leagues;
