import { Box } from '@mui/material';

import Layout from '../../components/Layout';

const Settings = () => {
  return (
    <Layout>
      <Box>Settings</Box>
    </Layout>
  );
};

export default Settings;
